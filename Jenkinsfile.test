pipeline {
		agent any
		stages {
			stage('Test') {
				steps {
					echo 'Installing dependencies and running tests...'
				}
			}
		}
		post {
			success {
				script {
					echo 'Triggering PR validation pipeline...'
					build job: "Terraform-PR-Pipeline/${env.BRANCH_NAME}",
						wait: false,
						parameters: [],
						propagateParameters: true,
						propagate: true
				}
			}
		}
}
