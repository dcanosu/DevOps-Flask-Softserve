pipeline {
		agent any
		stages {
			stage('Test') {
				steps {
					echo 'Installing dependencies and running tests...'
				}
			}
		}
		post {
			success {
				build job: 'Terraform-PR-Pipeline', propagate: true
			}
		}
}
