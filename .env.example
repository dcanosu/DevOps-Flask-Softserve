# Flask Application Secret Key
# IMPORTANT: Change this to a unique, random string in your actual .env file for production!
# For local development, this example key is fine to get started.
SECRET_KEY="a_secure_random_secret_key_should_be_here_for_production"

# PostgreSQL Database Connection URL
# This is the recommended way to configure the database.
# Replace YOUR_DB_USER, YOUR_DB_PASSWORD, and YOUR_DB_NAME with your actual credentials.
# Ensure the database 'YOUR_DB_NAME' exists on your PostgreSQL server.
# Format: postgresql://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL="postgresql://YOUR_DB_USER:YOUR_DB_PASSWORD@localhost:5432/YOUR_DB_NAME"

# --- Optional: Individual Database Components ---
# If DATABASE_URL is set above, these will be IGNORED by the application's default configuration.
# You can use these if you prefer not to set the full DATABASE_URL.
# DB_USER="flask_user"
# DB_PASSWORD="flask_password"
# DB_HOST="localhost"
# DB_PORT="5432"
# DB_NAME="flask_notes_db"
